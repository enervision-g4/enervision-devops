name: g4-etl

services:
  etl:
    image: ${IMAGE}
    container_name: g4_etl
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
    networks:
      - g4_net

networks:
  g4_net:
    external: true
