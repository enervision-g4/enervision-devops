---
apiVersion: v1
kind: Service
metadata:
  name: g4-api
  labels:
    app.kubernetes.io/name: api
spec:
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
